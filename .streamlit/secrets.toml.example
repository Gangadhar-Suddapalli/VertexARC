# For Streamlit Community Cloud deployment, add these in the app's
# "Secrets" settings (Settings -> Secrets), NOT committed to git.

OPENAI_API_KEY = "sk-your-openai-api-key-here"
OPENAI_CHAT_MODEL = "gpt-4o-mini"
OPENAI_EMBED_MODEL = "text-embedding-3-small"
