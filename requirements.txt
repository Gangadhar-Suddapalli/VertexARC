streamlit>=1.36.0
openai>=1.40.0
pypdf>=4.2.0
numpy>=1.26.0
fpdf2>=2.7.9
tiktoken>=0.7.0
sentence-transformers>=3.0.0
python-dotenv>=1.0.0
# Pinned floors so a fresh install can't regress into the old huggingface_hub /
# datasets incompatibility (missing HfFolder) seen during local setup.
datasets>=4.0.0
huggingface_hub>=1.0.0
